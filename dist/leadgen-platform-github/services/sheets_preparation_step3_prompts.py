"""
Промпти та резервні підказки для кроку 2 AI (Right Company Name / Right Title).

Назви компаній: `services/sheets_prep_data/company_name_training.csv`.
Посади: `services/sheets_prep_data/title_training.csv`.
Детальні правила пріоритету посад — `services/sheets_preparation_title_rules.py` (додаються до промпта завжди).
Якщо файлів немає — FALLBACK_* нижче.
"""

# Якщо CSV з еталонами відсутній
FALLBACK_COMPANY_HINT = """
Загальні правила для Right Company Name (розсилки):
- Читабельний вигляд: Title Case або збережений бренд; без зайвих «Inc.», «LLC», «B.V.», «AG» у кінці, якщо це не частина бренду.
- Прибрати зайві пробіли; виправити очевидні опечатки кодування (наприклад Kuepfer замість mojibake).
- Порожній вхід → порожній вихід.
"""

FALLBACK_TITLE_HINT = """
Загальні правила для Right Title (розсилки), якщо еталонний CSV відсутній:
- Канонічні скорочення: Chief Executive Officer → CEO, Chief Technology Officer → CTO тощо.
- Лише чиста посада без префіксів на кшталт CXO -, DIR -, GF -, VP - тощо.
- Не змінюй рівень посади (Director не замінюй на Head тощо), якщо немає чіткої підстави.
- Якщо не знаєш канонічної форми — поверни порожній right_title.
- Один тайтл у виході; символ & не використовувати; кілька ролей у вхідному рядку — одна за пріоритетом (див. блок TITLE_PRIORITY нижче в промпті).
- Читабельний регістр; без ЗАВЕЛИКОГО КАПСУ.
- Порожній вхід → порожній вихід.
"""
