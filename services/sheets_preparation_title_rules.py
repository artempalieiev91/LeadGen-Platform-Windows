"""
Правила пріоритету для Right Title (крок 2 AI, Sheets Preparation).
Канонічні форми — у `sheets_prep_data/title_training.csv` (основний словник).
Вихід — чистий тайтл без службових префіксів (без CXO-, DIR-, GF- тощо).
"""

from __future__ import annotations

# Текстовий блок для промпта OpenAI (українською; канонічні тайтли — як у колонці Right Title еталонів).
TITLE_PRIORITY_PROMPT_BLOCK = """
## Обов’язкові правила для поля right_title (додатково до еталонних пар вище)

**Чистий тайтл:** у `right_title` виводь **лише** канонічну посаду латиницею, **як у колонці Right Title** у еталонах (наприклад `CEO`, `Managing Director`, `VP of Operations`). **Не додавай** на початок жодних префіксів чи тегів: не пиши `CXO -`, `DIR -`, `VP -`, `GF -`, `MGR -`, `FND -`, `OWN -` та подібне — тільки сама посада.

**Словник:** форми бери з еталонів у цьому промпті (`title_training.csv`). Якщо вхід неоднозначний — обери канон, який уже є в еталонах для подібного випадку.

**Не знижуй і не міняй рівень посади без еталону:** якщо у вхідному Title є **Director** / **VP** / **Senior Director** тощо — у `right_title` має лишатися **відповідний рівень** з еталонів (наприклад *Director of Product*, *VP of …*). **Не** замінюй Director на **Head of …**, VP на **Manager**, якщо в парах для подібних формулювань у словнику зазначено Director/VP. *Head* і *Director* у еталонах — **різні** канони; не підміняй їх одне одним «на око».

**Якщо не впевнений:** немає близької пари в еталонах і не можеш чесно взяти **існуючий** канон зі словника — поверни **`right_title` порожнім** (краще порожньо, ніж вигаданий або помилковий тайтл).

**Один тайтл, без &:** у `right_title` — **рівно одна** посада. Символ **`&` не використовуй** у виході. Якщо у вхідному Title **дві окремі посади** (наприклад CEO & Founder) — залиш **одну** за пріоритетом нижче. Якщо ж `&` з’єднує **області однієї посади** (наприклад *Director of Product Strategy & Operations* — одна роль), не розбивай на дві конкуруючі посади: зістав з еталоном **того ж рівня** (у словнику для цього прикладу — *Director of Operations*).

**COO лише про операції:** `COO` / Chief Operating Officer — це **операційна** роль. **Не** зіставляй з COO посади, де домінує **технології** (без явного «Operating» / «Operations» / COO у вхідному рядку): наприклад *Executive Vice President Technology*, *VP Technology*, *SVP Engineering* тощо — для них канон з еталонів зазвичай `CTO`, `VP of Technology`, `VP of Engineering` або інший tech-тайтл, **не** COO.

**Пріоритет вибору однієї ролі** (коли кілька конкурують; вище — важливіше):
1. **CXO** — CEO, CTO, CFO, COO, CMO, CIO, CPO, CDO, CRO, Chief * Officer, Co-CEO; President — коли це явна C-level / головна роль у рядку.
2. **Director та VP** — Managing Director, Director …, VP …, Vice President …, Technical Director тощо з еталонів.
3. **Managers / Head** — Head of …, … Manager, Product Manager, General Manager (як у еталоні).
4. **Founder / Co-Founder** — **найнижчий** пріоритет серед «керівних», якщо в рядку є й CXO/Director/VP/Manager — обирай вищий рівень.
5. **Owner / Co-Owner** — **найнижчий** пріоритет; обирай лише якщо інших ролей зі словника для цього рядка немає.

Порожній вхідний Title → порожній `right_title`.
"""
